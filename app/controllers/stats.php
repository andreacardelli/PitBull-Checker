<?php

switch ($action) {
	default:
		// $checkIds = array();
		// foreach (glob(DIR_ROOT."/logs/".$_SESSION['user'].".*.csv") as $filename) {
	 //        $checkIds[]= end(explode('/', $filename));
	 //    }
	break;
}

?>